{
  "name": "lychee-basic-typescript-client",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "npm run build",
    "test": "npm run build && node --test dist/test/*.test.js",
    "start": "node dist/src/basic_client.js"
  },
  "devDependencies": {
    "@types/node": "^18.19.0",
    "typescript": "^5.8.0"
  }
}
