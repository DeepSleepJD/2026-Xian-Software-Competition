# 荔枝争运战 · 策略与注意点(feature/blockade-strategy)

> 本文档汇总当前策略、地图情报、机制坑点与待办。随迭代更新。
> 最近更新:2026-07-05。

---

## 0. 终极目标
**分数赢。** 原"我到你不到"的纯封锁在新图(单咽喉+分叉)不可靠(见 §3),现转向
**竞速 + 搜刮冲分 + 适度延迟对手** 的混合打法。

---

## 1. 计分模型(最重要,曾被严重低估)
服务端 score 上限构成(exe 配置):

| 分项 | 上限 | 来源 |
|---|---|---|
| **鲜度 freshnessScore** | **180** | 交付时鲜度越高越好(掉/HOT/被冻都拉低) |
| **好果 goodFruitScore** | **180** | 交付时手里好果数 |
| **任务 taskScore** | **180(封顶)** | 完成任务;封顶即止,不用贪全部 |
| 交付基数 deliveryScore | ~180 | 与任务完成度挂钩(fullTask=180) |
| 时间分 timeScore | 90 | 交付越早越高 |
| 未完成任务 | 80(封顶) | catchUp band [60,90,110] |

**结论:鲜度(180)+好果(180)和任务(180)一样大。快交付吃鲜度/时间、保好果、补鲜度(ICE_BOX)
是和刷任务同等重要的大矿——目前没充分利用(见 §7 TODO)。**

---

## 2. 地图情报(比赛方公开,server -m 指定)
- 文件:`server/map_config.json`(default)、`server/map_config_variant_a.json`(variant_a)。
- 两图均 15 节点;起点 S01,宫门(gate)S14,终点 S15。**reverify=S14,rushExcluded=[S11,S12,S13]。**

### 唯一咽喉 = S10(两图都是)
```
            ┌── S11 ──┐
   …→ S10 ─┤          ├── S14(宫门) → S15
            └── S13 ──┘
```
- **S10 是唯一硬割点**(进入 {S11,S12,S13,S14} 只能经 S10:E `S10→S11`、`S10→S13`)。
- S10 之后**分叉**(S11 / S13 二选一),只在宫门 S14 重汇(捷径 E23 `S11→S14`、E24 `S10→S13`)。
- S10 入边:`S08→S10`、`S09→S10`。

### 任务分布(整局约19实例,会刷新;单项多为30分,后期出15分)
- **前半 S01–S09:约 330 分**(S03:90, S04/S05/S07/S08 各60)。
- **后半 S10–S13:约 195 分**(S10:90, S11:60, S12:30, S13:15)。
- 任务封顶180,所以"前半顺手 + 后半搜刮"凑满即可。

### 资源(effectType)
| 资源 | 效果 | 位置(default) |
|---|---|---|
| ICE_BOX | **鲜度+10**(直接加分) | S03,S06,S07 |
| FAST_HORSE | 移动 ×1.25 / 20回合 | S09 |
| SHORT_HORSE | 移动 ×1.18 / 14回合 | S04,S07,S08 |
| OFFICIAL_PERMIT | 出牌成本(赢猜拳) | S09,S13 |
| PASS_TOKEN | 出牌成本(**非破卡**) | S03,S08,S13 |
| INTEL | 揭雾 REVEAL_NODE(dist≤15) | 多处 |
| BOAT_RIGHT | 水路/DOCK 标记 | S04 |

### 障碍
- default:S06,S08,S10,S11;variant:S06,**S07**,S10,S11。
- variant 与 default 差异:边 `S03→S06` 换成 `S02→S06`;距离普涨、**山路更长**(S06→S08 58, S08→S10 54);
  FAST_HORSE 挪到 S06;资源略有洗牌。**结构(S10唯一割点+分叉)一致。**

---

## 3. 机制与坑点(GOTCHAS — 血泪教训)
1. **最多同时 2 张卡**;**自己的卡也挡自己**(设了宫门卡自己也过不去)。
2. **单卡封不死**:对手可 **FORCED_PASS** 穿过;**不能连续两次 FORCED_PASS**(FORCED_PASS_REPEAT 被拒),
   且 forcepass 有**时间税**。要"连续两个相邻必经点"才卡死——**本图分叉,没有相邻双必经点 → 2卡封不死(地图专门克制)。**
3. **frozen mid-edge 无法 forcepass/break_guard**(都需当前节点):你一旦 **commit 进"目标被卡"的边**,
   就冻在边上、只能等风化(S10 是 KEY_PASS,风化久 ~225帧,基本废)。**所以先到 S10 者 camp、后到者被冻——S10 竞速是生死线。**
4. **CLEAR ≠ T04(两个东西,别搞混!)**:
   - `CLEAR`(主车队):只清障、**不得分**。
   - `T04`=`CLEAR_OBSTACLE` 任务:`CLAIM_TASK` **清障 + 得30分**。
   - **规则:障碍带 T04 就 `CLAIM_TASK`(得分),没有才 `CLEAR`。小分队清 T04 障碍会白丢30分(TASK_TARGET_LOST)。**
   - ⚠ 当前"超时自清"用的是纯 `CLEAR`,遇 T04 会丢分 → **待修(§7)**。
5. **BREAK_GUARD 带 goodFruit/badFruit 字段 → 服务端 INVALID_JSON 退赛**;裸 BREAK_GUARD 被接受但0伤害。已弃用。
6. **CLAIM_TASK / CLAIM_RESOURCE / PROCESS 都是独立"读条",串行、非免费**:处理中(PROCESSING)发不了第二个主车队动作;
   领任务≈3-4帧读条,处理站≈4-7帧。**领任务永远花额外帧,不存在"处理时顺手免费领"。**
7. **动作额度**:1 主车队 + 1 window-card(WINDOW_CARD)+ 1 小分队(SQUAD_*)可**同帧并行**(不同额度);
   主车队动作之间不行。**请求体主队动作放最前**(服务端 CLAIM_TASK 排序 bug)。
8. **SQUAD_SCOUT**(人力1,便宜):放 scout marker → 该节点**处理/领取回合 −3(下限2)、持续45回合**,
   覆盖 CLAIM/BOARD/各TRANSFER/CLEAR_OBSTACLE/VERIFY。**= 提速工具(目前未接入)。**
9. **小分队人力**:CLEAR/WEAKEN/REINFORCE 各2、SCOUT 1;是有限池,**耗尽即罢工**(曾致主队在障碍前原地不动)。
10. **守卫检测**用 `node.guard.{active,defense,ownerTeamId}`(不是 effectiveCombatCount,那个恒0)。
11. **猜拳**:XIAN_GONG(献贡)克 YAN_DIE/BING_ZHENG,平自己,只输 QIANG_XING(需马);成本=鲜度≥80+1好果/拍。
    但**好果直接算分(§1)**,天天出 XIAN_GONG 会烧好果分,需权衡(§7)。
12. **HOT 天气只加快鲜度流失**(不影响移动);HEAVY_RAIN 水路×1.35、MOUNTAIN_FOG 山路×1.10(影响移动)。

---

## 4. 当前策略(_main_action 优先级阶梯)
1. **忙/在边上** → 交给引擎 / 继续 move(边上不能设卡/处理)。
2. **verified** → 冲终点交付。
3. **交付死线**(我方精确 ETA,不掺对手/天气)→ 冲宫门。
4. **抢 S10(恒 race)** → `_camp_choke` 始终把 S10 当竞速目标;到达 S10 时若 `_lead_at ≥ RACE_LEAD(8)`
   就 **set-and-go 落一道延迟卡**(逼对手 forcepass 交税)然后走,**不 camp 空等**。
5. **后半搜刮** → 无可拦截时,趁交付余量绕去最近可领任务刷分(`_scavenge_target`),死线紧了再交付。

**破卡防死锁**:被敌卡挡在绕不开的节点(割点)→ FORCED_PASS 穿过(单次,和 wait 交替,不连发)。
**障碍防呆**:下一跳障碍 → 等小分队清;超时(8帧)**主队自清**,永不卡死。
**出牌**:能合法出就出 XIAN_GONG(鲜度≥80+好果>1)。
**小分队**:目前只**清障**(路径2跳内、just-in-time);scout/reinforce/weaken 未接。

---

## 5. 当前战力(default 图,seed 20260618)
| 对手 | 结果 | 说明 |
|---|---|---|
| l1-demo | **722 vs 416 稳胜** | 抢S10→延迟卡→后半搜刮(任务顶满180),移动顺畅 |
| fix 分支(封锁版) | **444 vs 684 负** | S10 竞速被 fix 险胜~2帧 → 冻我方边上(破卡后能送达,不再0) |

- **worktree 对手**:`scratchpad/wt/fix`(origin/feature/blockade-strategy-fix)、`scratchpad/wt/camp`(pivot前的camp-freeze版)。
- variant_a 图:仅初步(未系统 PK)。

---

## 6. 调测 / PK 工具(scratchpad)
- `pk.sh LOG RTM PORT SEED MAP OPP` — ours vs demo/内置策略,MAP=default|variant。
- `pk2.sh LOG PORT SEED MAP OPP_PY OPP_NAME` — ours vs 另一 worktree 的客户端(打 fix/camp)。
- server 指定地图:`lychee-arena-server.exe … -m map_config.json`。
- 结果看 server 目录 `data.csv`;逐帧看 `--record-dir` 的 jsonl。
- ⚠ **多局并发会共用 server/data.csv 互相覆盖 → 必须串行跑。**

---

## 7. 已知问题 / TODO(按价值排序)
1. **[大矿] 利用 鲜度+好果+时间(共~450分)**:快交付吃鲜度/时间、抢 ICE_BOX 补鲜度、
   评估 XIAN_GONG 烧好果是否划算(需测好果→分换算曲线)。可能比封锁更能拉分,且对任何对手都涨分。
2. **[bug] 自清障碍应优先 T04-CLAIM**(§3.4):当前纯 CLEAR 遇 T04 丢30分。改:有T04→CLAIM_TASK,否则CLEAR;
   小分队也别清 T04 障碍(留给主队得分)。
3. **[硬骨头] vs 封锁对手的 S10 竞速+冻结**:单咽喉先到者通吃,我们险负被冻。
   方向:抢马时序/探路减帧赢先手;或落后时**别 commit 进被卡的边**(不送人头),转拖分。
4. **[未接] SQUAD_SCOUT 提速**(处理/领取 −3):前半探路减处理帧、后半加速领任务。人力要和清障平衡。
5. **[未接] 前半顺手等价刷分**:冲 S10 途中顺手捡不拖竞速的 ICE_BOX/on-path任务/资源。
6. **[自适应,暂缓]** 判对手强弱=会不会破卡;难判就默认"强"。当前默认强(不靠白嫖封杀)。
7. **[稳健] 共占节点死锁**:双方同节点互相 contest 会卡 PROCESS(自play产物,vs分道对手不现);
   已加 anti-stuck,但服务端强制共占时 MOVE 也被拒,未完全解。
8. **variant_a 图系统 PK** 未做。
9. squad reinforce/weaken(加固/削卡)未接;拉不开帧差时的对策(改道/求和)未定。

---

## 8. 演进史(简)
纯封锁(camp-freeze,vs demo 618封杀,脆)→ 发现新图分叉+2卡封不死 → 转 **set-and-go延迟+后半搜刮**
(vs demo 661→722)→ PK 发现被封锁对手**冻死到0** → 加**破卡forcepass** + **恒抢S10**(vs fix 80→444)
→ 修**障碍原地不动**(自清,vs demo →722)。**下一步挖鲜度/好果大矿。**
